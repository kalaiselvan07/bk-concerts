# Add these to your backend environment (Go application runtime)
# You can set these directly in your OS or load them via a .env file helper in Go.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=blackticketentertainments.ge@gmail.com
SMTP_PASS=rpfofriipdmjrodp

